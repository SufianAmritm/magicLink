export enum API_CONSTANTS {
  JWT = 'JWT',
  PROJECT_NAME = 'Magic Link',
}
export enum RESPONSE_MESSAGE {
  FORBIDDEN = 'You are not authorized to perform this operation',
  INTERNAL_SERVER_ERROR = 'Something went wrong with the operation. We are sorry for inconvenience, please try again letter.',
}
export enum NODE_ENV {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  TEST = 'test',
}
